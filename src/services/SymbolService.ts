// Native: symbols come from the bundled zip extracted to
// FileSystem.documentDirectory/symbols/{library}/{id}.webp (§9.2).
// Extraction ships with the native builds (Phase 2) — until then,
// native renders text-only buttons.
export function getSymbolUri(_ref: string): string | null {
  return null
}
